# Optional personal overrides for one workstation.
# Copy to .env.local only for notes/tools that explicitly load it.
# NIDO runtime loads .env, backend/.env and frontend/.env; keep real secrets in .env.

# Example:
# PORT=5001
# CLIENT_URLS="http://localhost:5173,http://127.0.0.1:5173"
